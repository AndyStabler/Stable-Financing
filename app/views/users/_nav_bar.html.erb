<nav id="main-nav" class="navbar navbar-default" role="navigation">
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <div id="balance-update-container" class="container-fluid">
        <div class="container">
          <div class="row">
            <div class="col-xs-1 col-md-2">
              <div class="navbar-header">
                <%= link_to current_user.name, current_user, :class => "navbar-brand" %>
              </div>
            </div>

            <div class="col-xs-9 col-md-7 navbar-form pull-left">
              <%= form_for(balance,
                :remote => true,
                :method => :post,
                :url => new_balance_path) do |f| %>
                  <div class="input-group">
                    <span class="input-group-addon">£</span>
                    <%= f.number_field :value, placeholder: number_with_precision(balance.value, precision: 2), :class => "form-control", :value => "" %>
                    <span class="input-group-btn">
                        <%= f.button(type: 'submit', class: "btn btn-default") do %>
                          Update Balance
                        <% end %>
                    </span>
                  </div>
              <% end %>
            </div>
            <div class="col-xs-2 col-md-3 pull-right">
              <!-- Collect the nav links, forms, and other content for toggling -->
              <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><a href="#">Something else here</a></li>
                    <li role="separator" class="divider"></li>
                    <li><a href="#">Log out</a></li>
                  </ul>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
</nav>
