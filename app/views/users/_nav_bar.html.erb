<nav id="main-nav" class="navbar navbar-default" style="border-radius: 0;">
  <div id="balance-update-container"class="container-fluid">
    <div class="navbar-header">
      <%= link_to current_user.name, current_user, :class => "navbar-brand jura-font" %>
    </div>
    <div class="collapse navbar-collapse">
      <div class="navbar-form navbar-left">
        <%= form_for(balance,
            :remote => true,
            :method => :post,
            :url => new_balance_path) do |f| %>
          <div class="input-group">
            <span class="input-group-addon">£</span>
            <%= f.number_field :value, placeholder: number_with_precision(balance.value, precision: 2), :class => "form-control", :value => "" %>
            <span class="input-group-btn">
              <%= f.button(type: 'submit', class: "btn btn-default") do %>
                Update Balance
              <% end %>
            </span>
          </div>
        <% end %>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Settings <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Something else here</a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
