<%= render 'balances/nav_bar', :balance => @balance %>

<p id="notice"><%= notice %></p>

<div class="container">
  <div class="user-transfers col-md-3">
    <h2>Transfers</h2>

    <div class="user-transfers-new">
      <%= render 'transfers/new_transfer_toggle', transfer: @transfer %>
      <%= render 'transfers/new_transfer_form', transfer: @transfer %>
    </div>
    <%= render "transfers/transfers_list", user: current_user %>
  </div>
  <div class="col-md-9">
    <div>
      <%= link_to "Download csv", user_path(current_user, :format => :csv) %>
    </div>
    <div id="balance-data">
    </div>
    <div id="selected-balance-info" style="float:left;">
    </div>
  </div>
</div>
<div class="user-data"
  data-json-url="<%= user_path current_user %>.json"
  data-transfers-url="<%= transfers_path current_user %>"
</div>

<div id="chart_div"></div>
<!--Load the AJAX API-->
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
