# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  'on': 2015-07-24
  amount: 9.99
  outgoing: false
  recurrence: <%= Transfer.recurrences[:no] %>
  reference: ref 1
  user: andy

two:
  'on': 2015-07-24
  amount: 9.99
  outgoing: true
  recurrence: <%= Transfer.recurrences[:no] %>
  reference: ref 2
  user: fiona

# Incoming

wages_in:
  'on': <%= (DateTime.now-1.month).beginning_of_month+24.days %>
  amount: 100
  outgoing: false
  recurrence: <%= Transfer.recurrences[:monthly] %>
  reference: wages
  user: andy

rent_from_fi:
  'on': <%= (DateTime.now-1.month).end_of_month-1.day %>
  amount: 100
  outgoing: false
  recurrence: <%= Transfer.recurrences[:monthly] %>
  reference: rent from Fi
  user: andy

pocket_money:
  'on': <%= (DateTime.now-1.month).beginning_of_month %>
  amount: 2
  outgoing: false
  recurrence: <%= Transfer.recurrences[:weekly] %>
  reference: pocket money
  user: andy

# Outgoing

rent:
  'on': <%= (DateTime.now-1.month).end_of_month %>
  amount: 200
  outgoing: true
  recurrence: <%= Transfer.recurrences[:monthly] %>
  reference: rent
  user: andy

phone:
  'on': <%= (DateTime.now-1.month).end_of_month %>
  amount: 50
  outgoing: true
  recurrence: <%= Transfer.recurrences[:weekly] %>
  reference: phone
  user: andy

lunch:
  'on': <%= DateTime.now %>
  amount: 10
  outgoing: true
  recurrence: <%= Transfer.recurrences[:daily] %>
  reference: lunch
  user: andy

plumber:
  'on': <%= DateTime.now+1.day %>
  amount: 50
  outgoing: true
  recurrence: <%= Transfer.recurrences[:no] %>
  reference: plumber
  user: andy
